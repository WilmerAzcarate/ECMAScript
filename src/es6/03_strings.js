// @ts-check

let hello = 'Hello';
let world = "World";

let epicPhrase = `${hello} ${world}!`;
console.log(epicPhrase);

let multilinePhrase = `Esta es una frase epica
la continuacion de una frase epica.
`;
console.log(multilinePhrase);